[project]
name = "syntax-typer-backend"
version = "0.2.0"
description = "Syntax Typer — FastAPI backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.1",
    "pluggy>=1.6.0",
    "pyyaml>=6.0.3",
    "sqlalchemy>=2.0.49",
    "tree-sitter>=0.25.2",
    "uvicorn[standard]>=0.46.0",
]
